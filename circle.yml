machine:
  node:
    version: 5.3.0

general:
  branches:
    ignore:
      - gh-pages

dependencies:
  cache_directories:
    - "~/.npm"
    - "node_modules"

  override:
    - npm install

test:
  override:
    - npm test
